rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Multi-tenant / app collection access
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
